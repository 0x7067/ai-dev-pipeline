# Spec index. Generated/maintained alongside per-spec docs/specs/<id>/spec.yaml.
# Parsed via scripts/specs/parse-spec.sh (parse_spec_index).
specs:
  - id: decision-surfacing
    title: Decision-Surfacing Directive (Planner + Researcher)
    status: accepted
    risk: low
    tags:
      - policy
      - planner
      - researcher
      - hitl
  - id: run-id-isolation
    title: Run-ID-Based Artifact Isolation
    status: accepted
    risk: medium
    tags:
      - runs
      - boundary
      - parallelism
  - id: zero-setup-install
    title: Zero-Setup Install
    status: accepted
    risk: medium
    tags:
      - install
      - boundary
      - resolver
