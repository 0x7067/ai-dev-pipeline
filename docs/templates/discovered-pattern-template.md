# Discovered Pattern: {{pattern_id}}

<!-- Auto-generated by /audit standards. Do not edit by hand. -->
<!-- Promote to .claude/rules/ via the discoverer agent to make it policy. -->

## Pattern ID

`{{pattern_id}}`

## Bucket

`{{bucket}}`

## Feature

`{{feature}}`

## Observed Convention

**Value:** `{{value}}`
**Confidence:** {{confidence_pct}}% ({{band}})
**Frequency:** {{frequency_pct}}% of files in bucket ({{count}} / {{total}})

## Description

This pattern was detected in the `{{bucket}}` bucket by `/audit standards`.
`{{count}}` out of `{{total}}` tracked files in this bucket exhibit the
`{{feature}} = {{value}}` convention, giving a confidence of {{confidence_pct}}%.

## Scanned At

`{{scanned_at}}`

## Status

- [ ] Reviewed
- [ ] Promoted to `.claude/rules/`

## Promotion Notes

_Fill in when promoting via the discoverer agent._

<!-- When promoted, the discoverer agent writes a policy block to .claude/rules/<name>.md -->
